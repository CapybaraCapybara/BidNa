name: bidna
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^5.0.0 # หรือเวอร์ชันล่าสุด
  firebase_core: ^3.15.2
  firebase_auth: ^5.7.0
  google_sign_in: ^7.2.0
  image_picker: ^1.2.1
  image: ^4.7.2
  intl: ^0.20.2
  confetti: ^0.8.0
  fake_cloud_firestore: ^3.0.3
  firebase_auth_mocks: ^0.14.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  fake_cloud_firestore: ^3.0.0

flutter:
  uses-material-design: true
